// Renders battery state pushed from amr_charging via amr_command.

const panel = document.getElementById('battery-panel');

export function renderBattery(batteryFrame) {
  // TODO: panel.textContent = `${batteryFrame.percentage}%`, etc.
}
